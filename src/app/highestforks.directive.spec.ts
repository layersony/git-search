import { HighestforksDirective } from './highestforks.directive';

describe('HighestforksDirective', () => {
  it('should create an instance', () => {
    const directive = new HighestforksDirective();
    expect(directive).toBeTruthy();
  });
});
